[build]
  base = "vendedor-pro"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Necessário para o React Router funcionar em URLs diretas
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
